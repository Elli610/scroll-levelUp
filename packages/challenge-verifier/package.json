{
  "name": "challenge-verifier",
  "version": "0.1.0",
  "description": "A simple rust script to verify the submitted levelUP challenges",
  "private": false,
  "scripts": {
    "build": "cargo build",
    "test": "cargo test",
    "fmt": "cargo clippy --all-targets --all-features -- -D warnings && cargo fmt",
    "fmt:check": "cargo fmt -- --check",
    "clean": "cargo clean"
  },
  "author": "Elli610 <nathan@cypherlab.fr>",
  "license": "MIT"
}
